function attributeMissions(missions) {

    //console.log(missions)
  var vitesse = 15;
  var fin = 50;
  var Masseur = new Array();
  var index = new Array;
  var value;
  
    class monObjet {
      // Special constructor method
      constructor(value, coordinateX, coordinateY) {
          // Set properties
          this.value=1;
          this.coordinateX = 0;
            this.coordinateY = 0;
  
        }
  }
  
  for(i = 1; i < fin ; i++){
  Masseur.push(new monObjet);
  var maValeur=Masseur.push(monObjet.value);
  maValeur=i;
  //Masseur.push(monObjet.coordinateX=missions[0].x);
  //Masseur.push(monObjet.coordinateY=missions[0].y);
  }
  
  console.log(Masseur);
  
  
  
  setInterval(() => attributeMissions(start, length), 300000);
  
  
  
  
    return {}
}



function attributeMissions(missions) {

    //console.log(missions)
    var vitesse = 15;
    var fin = 50;
    var Masseur = new Array();
    var index = new Array;
    var value;
  
    class monObjet {
        // Special constructor method
        constructor(value, coordinateX, coordinateY) {
            // Set properties
            this.value=1;
            this.coordinateX = 0;
            this.coordinateY = 0;
  
        }
    }
  
    for(i = 1; i < fin ; i++){
        Masseur.push(new monObjet);
        var maValeur=Masseur.push(monObjet.value);
        maValeur=i;
        //Masseur.push(monObjet.coordinateX=missions[0].x);
        //Masseur.push(monObjet.coordinateY=missions[0].y);
    }
  
    console.log(Masseur);
  
    setInterval(() => attributeMissions(start, length), 300000);
  
    return {}
}




// write your code here
// HINT: you have access to the `console` object for debugging



var maPosition = {
    x: 0, 
    y: 0
  }
  
  var LaMissionAComparer = {
    start: 835, 
    length: 5, 
    x: 0.8122583789483786, 
    y: 2.499506048786145
  }
  
  
  var currentid; 
  
  for(i=0; i<monTableauDeMission.length; i++){
    currentid = [i];
    TrouverLaMissionLaPlusProche(monTableauDeMission[i]);
      
  }
  var ResultatDistanceMasseurs = [];
  
  function identifierLeMasseur(){
    // je veux match la pproprité de maMissionCalculé.currentId avec MontableauDeMsion.id
  
  }
  
  
  function trieDuTableau (){
    // tu trie ici
  
    // tu récupere la plus petite valeur
  
  }
   
  function TrouverLaMissionLaPlusProche(LaMissionAComparer) {
  
  
    
    //console.log(missions)
    var vitesse = 15;
    //var index = new Array;
    var value;
    var resultatKm; 
  
    fonction calculDesDistances(){
      // récuperer ma positoion et faire le calcul de la distance avec la missionacomparer
  
      // ici tu calcul la distance entre ton ta position et la mision 
      resultatKm = ResultatMathématiqueDuCalculDesCordonnée  
    }
  
    var resultatKm = unNombre;
    var maMissionCalculé = {
      id : currentid,
      km : resultatKm
    }
  
    ResultatDistanceMasseurs.push(maMissionCalculé);
  
  
  }